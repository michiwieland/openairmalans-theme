<article class="single-impression">

	{% set impressions = function( 'carbon_get_post_meta', post.ID, 'crb_image', 'complex' ) %}

	{% for impressions in impressions %}

        <img src="{{ impressions.image }}">

	{% endfor %}

</article>
