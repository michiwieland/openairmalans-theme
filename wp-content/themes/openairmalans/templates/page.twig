{% include 'header.twig' %}

	{% for post in posts %}

		<article>

			<header>
				<h2>{{ post.title }}</h2>
			</header>

			<div class="content">
				{{ post.get_content() }}
			</div>

		</article>

	{% endfor %}

{% include 'footer.twig' %}